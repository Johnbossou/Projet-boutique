# Dossier `src/` (services utilitaires)

L’application active utilise **Expo Router** dans `app/`.

Ce dossier conserve uniquement :

- `services/api.js` — client Axios (optionnel, pour scripts ou réutilisation)

Les anciens écrans React Navigation ont été supprimés (migration terminée vers `app/`).
